# Debian Test Environment for curllm
FROM debian:12@sha256:3be3c43cd7ee541bb601f8131132ed025ff97406130f73774fe3ffcf6ee8b86d

ENV DEBIAN_FRONTEND=noninteractive
ENV PYTHONUNBUFFERED=1

RUN apt-get update && apt-get install -y \
    python3 \
    python3-pip \
    python3-venv \
    curl \
    wget \
    git \
    build-essential \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

RUN useradd -m -s /bin/bash testuser
WORKDIR /test
RUN chown testuser:testuser /test
USER testuser

COPY --chown=testuser:testuser test_pypi_install.sh /test/
RUN chmod +x /test/test_pypi_install.sh

CMD ["/test/test_pypi_install.sh"]
